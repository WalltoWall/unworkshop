{
	"name": "unworkshop",
	"private": true,
	"type": "module",
	"workspaces": ["packages/*"],
	"scripts": {
		"lint": "bunx -b biome check . --fix",
		"format": "bunx -b biome format . --write",
		"dev:app": "cd ./packages/app && bun run dev"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"typescript": "^5.8.3"
	}
}
